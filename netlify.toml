[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

[[scheduled]]
  function = "ema-scheduler"
  schedule = "*/5 * * * *"
